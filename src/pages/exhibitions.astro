---
import BaseLayout from '@layouts/BaseLayout.astro';
import museumsData from '../data/museums.json';

const base = import.meta.env.BASE_URL;
const museums = museumsData;
---

<BaseLayout
  title="常設展示 — WESON MUSEUM | VRChat VR美術館"
  description="WESON MUSEUMの常設展示。VRChat上に開かれた4つの美術館——天空・パラオ・森・雨降る——それぞれが固有の空気と演出を持つ空間です。"
  descriptionEn="WESON MUSEUM's permanent exhibitions on VRChat: four unique galleries — Sky, Palau, Forest, and Rain — each with its own atmosphere and VR-exclusive presentation. The Palau Museum is operated under the patronage of the Embassy of the Republic of Palau."
>

  <!-- Hero -->
  <section class="ex-hero">
    <div class="container">
      <nav class="breadcrumb" aria-label="パンくずリスト">
        <ol>
          <li><a href="/">Home</a></li>
          <li aria-current="page">Exhibitions</li>
        </ol>
      </nav>

      <p class="ex-eyebrow mono-label">Permanent Exhibitions — VRChat</p>
      <h1 class="ex-h1">Exhibitions</h1>
      <p class="ex-lead">
        WESON MUSEUM は、VRChat 上に4つの常設美術館を持ちます。<br class="br-md" />
        それぞれ異なる空気と演出を持つ空間で、作品の展示が継続的に行われています。
      </p>

      <div class="tour-note glass">
        <span class="status-dot" aria-hidden="true"></span>
        <p class="mono-label">
          VR在廊ツアー — 毎週土曜日 21:00〜22:00 ／ VRChat上の各館を巡回開催
        </p>
      </div>
    </div>
  </section>

  <!-- Museums -->
  <div class="museums">
    {museums.map((m, i) => (
      <section
        id={`museum-${m.num}`}
        class={`museum-section${i % 2 === 1 ? ' museum-section--alt' : ''}`}
        aria-labelledby={`museum-heading-${m.num}`}
      >
        <div class="container museum-layout">

          <div class="museum-img-col">
            <div class="museum-img-wrap">
              <img
                src={`${base}images/museums/${m.img}`}
                alt={m.ja}
                loading="lazy"
                decoding="async"
                class={m.status === 'renovation' ? 'img-renovation' : ''}
              />
              {m.status === 'renovation' && (
                <div class="closed-badge mono-label" aria-label="リニューアル予定">
                  Renovation Planned
                </div>
              )}
            </div>
          </div>

          <div class="museum-text-col">
            <div class="museum-num mono-label">{m.num}</div>
            <span class="accent-line"></span>
            <h2 id={`museum-heading-${m.num}`} class="museum-ja">{m.ja}</h2>
            <p class="museum-en mono-label">{m.en}{m.since && ` — Est. ${m.since}`}</p>
            <p class="museum-body">{m.bodyJa}</p>
            {m.bodyPau && (
              <p class="museum-body-en" lang="en">{m.bodyEn}</p>
            )}

            <a
              href={m.url}
              target="_blank"
              rel="noopener noreferrer"
              class="btn btn-outline museum-btn"
            >
              VRChatで入場する
              <span class="btn-icon" aria-hidden="true">↗</span>
            </a>
          </div>

        </div>
      </section>
    ))}
  </div>

  <!-- CTA -->
  <section class="ex-cta" aria-label="関連ページへの導線">
    <div class="container">
      <div class="cta-divider" aria-hidden="true"></div>
      <p class="cta-lead mono-label">Explore WESON MUSEUM</p>
      <nav aria-label="関連ページへのリンク">
        <ul class="cta-links">
          <li>
            <a href="/artlink/" class="btn btn-outline">
              ARTLINK
              <span class="cta-link-sub">企画展を見る</span>
            </a>
          </li>
          <li>
            <a href="/about/" class="btn btn-outline">
              About
              <span class="cta-link-sub">美術館について</span>
            </a>
          </li>
          <li>
            <a href="/news/" class="btn btn-outline">
              News
              <span class="cta-link-sub">最新情報</span>
            </a>
          </li>
        </ul>
      </nav>
    </div>
  </section>

</BaseLayout>

<style>
  /* ================================================================
     Hero
     ================================================================ */
  .ex-hero {
    padding-block: var(--space-16) var(--space-12);
    border-bottom: 1px solid var(--color-border);
  }

  .ex-eyebrow {
    color: var(--color-main);
    margin-bottom: var(--space-4);
  }

  .ex-h1 {
    font-size: clamp(2.4rem, 7vw, 4rem);
    font-weight: 200;
    letter-spacing: var(--tracking-widest);
    margin-bottom: var(--space-6);
  }

  .ex-lead {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    line-height: 2;
    max-width: var(--content-width);
    margin-bottom: var(--space-10);
  }

  .br-md { display: none; }
  @media (min-width: 640px) { .br-md { display: block; } }

  .tour-note {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-6);
    border-radius: var(--radius-md);
  }

  .tour-note .mono-label {
    color: var(--color-main);
    letter-spacing: var(--tracking-wide);
  }

  .status-dot {
    display: inline-block;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-main);
    box-shadow: var(--glow-main);
    flex-shrink: 0;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }

  /* ================================================================
     Museum Sections
     ================================================================ */
  .museum-section {
    padding-block: var(--space-16);
    border-bottom: 1px solid var(--color-border);
  }

  .museum-section--alt {
    background: var(--color-bg-1);
  }

  .museum-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-12);
    align-items: center;
  }

  /* 偶数館は画像と文を逆順に */
  .museum-section--alt .museum-img-col {
    order: 2;
  }

  .museum-section--alt .museum-text-col {
    order: 1;
  }

  @media (max-width: 768px) {
    .museum-layout {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }

    .museum-section--alt .museum-img-col,
    .museum-section--alt .museum-text-col {
      order: unset;
    }

    .museum-section {
      padding-block: var(--space-12);
    }
  }

  /* ---- 画像 ---- */
  .museum-img-wrap {
    position: relative;
    aspect-ratio: 4 / 3;
    border-radius: var(--radius-md);
    overflow: hidden;
    border: 1px solid var(--color-border);
  }

  .museum-img-wrap img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    filter: brightness(0.8) saturate(0.9);
    transition: transform var(--transition-slow), filter var(--transition-slow);
  }

  .museum-img-wrap:hover img {
    transform: scale(1.03);
    filter: brightness(0.9) saturate(1.05);
  }

  .img-renovation {
    filter: brightness(0.7) saturate(0.6) !important;
  }

  .closed-badge {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: rgba(5, 11, 10, 0.85);
    border: 1px solid var(--color-border-bright);
    color: var(--color-text-muted);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-sm);
    white-space: nowrap;
    letter-spacing: var(--tracking-widest);
  }

  /* ---- テキスト ---- */
  .museum-text-col {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .museum-num {
    color: var(--color-main);
  }

  .museum-ja {
    font-size: var(--text-2xl);
    font-weight: 200;
    letter-spacing: var(--tracking-wider);
    line-height: 1.2;
  }

  .museum-en {
    color: var(--color-text-muted);
    letter-spacing: var(--tracking-widest);
    margin-top: calc(-1 * var(--space-2));
  }

  .museum-body {
    font-size: var(--text-base);
    color: var(--color-text-muted);
    line-height: 2;
    margin-top: var(--space-2);
  }

  .museum-body-en {
    font-size: var(--text-sm);
    color: var(--color-text-subtle);
    line-height: 1.9;
    border-top: 1px solid var(--color-border);
    padding-top: var(--space-4);
    margin-top: var(--space-2);
  }

  .museum-btn {
    align-self: flex-start;
    margin-top: var(--space-2);
  }

  .btn-icon {
    font-size: var(--text-sm);
    color: var(--color-main);
  }

  /* ================================================================
     CTA
     ================================================================ */
  .ex-cta {
    padding-block: var(--space-24);
  }

  @media (max-width: 640px) {
    .ex-cta {
      padding-block: var(--space-16);
    }
  }

  .btn {
    display: inline-flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .cta-link-sub {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    letter-spacing: var(--tracking-normal);
    text-transform: none;
    color: var(--color-text-muted);
  }
</style>
